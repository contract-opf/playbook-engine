Use the playbook-from-corpus skill to help me create a negotiation playbook
from my company's real agreements. This is my first time using this project —
treat me as a new administrative user: interview me for everything you need
(where my agreement files are, what agreement type this is, which party names
are us, our counterparties, whether we have a baseline template, where output
should go) before running anything, and explain each stage as we go. I will
act as the judge where the skill calls for judgment.

Two standing rules: the contents of my agreements are confidential — never
commit them, never quote them into GitHub issues or anything that leaves this
machine; and keep all run artifacts in a gitignored directory (verify with
`git check-ignore` before writing).
